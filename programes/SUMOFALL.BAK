#include<stdio.h>
#include<conio.h>

int main(){
	 int num,sum=0,i;
	 clrscr();
	printf("Enter a positive number: ");
	scanf("%d",&num);

	 if(num<=0){
		printf("Invalid input. Please enter a positive number.\n");
	 }
	 else{
		for(i=1;i<=num;i++)
		{
		sum+=i;
		}
	 printf("The sum of all natural numbers up %d is %d\n",num,sum);
	 }
getch();
return 0;
}