#include<iostream.h>
#include<conio.h>

class geometry
{
	int c,b;
	int area(int a)
		{
		c=b=a;
		return(c*b);
		}
	int area(int a,int p)
		{
		c=a;
		b=p;
		return(c*b);
		}
	int volume(int a)
		{
		c=a;
		return(c*c*c);
		}
};
void main()
{
clrscr();
geometry g;
g.area(10);
g.area(20,30);
g.volume(6);
getch();
}
